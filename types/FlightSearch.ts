// Define the FlightSearchParams interface or type
export interface FlightSearchParams {
  origin?: string;
  destination?: string;
  departure_id: string;
  arrival_id: string;
  outbound_date: string;
  // Add other fields as needed
}
